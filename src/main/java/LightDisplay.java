public class LightDisplay {

    /* 2D array representing the grid of lights that
     * make up the light display
     */
    private boolean[][] lightGrid;
    
    /* Constructor for LightDisplay. Assume rows 
     * and cols are both greater than 0.
     * Each light has a 50% probability
     * of being turned on (i.e. set to true)
     */
    public LightDisplay(int rows, int cols)
    {
        // public lightGrid(int r, int c) {
            // if (lightGrid[r][c] == true)
                // return true;
            // else if 
                // if (Math.random() * 2 > 5)
                    // light[r][c] == true;
        // }
        
    }
    
    /* Analyzes the light at the row index and column index
     * given by the input parameters. Assume that lightRow
     * and lightCol are valid indices of lightGrid.
     */
    public boolean lightAnalysis(int lightRow, int lightCol)
    {
        // if (lightCol == true) > 2 || (lightCol == false) > 2
            // return true;
        // else 
            // return false;
        return true;
    }
    
    /* Other methods, variables, or constructors may be
     * present, but are not shown */
}